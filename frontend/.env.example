# Base URL for the ResolveAI backend API.
# When unset, the app uses the relative "/api" prefix, which the Vite dev
# server proxies to the backend (see vite.config.ts). Set this when the
# backend is served from a different origin.
VITE_API_BASE_URL=/api
